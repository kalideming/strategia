import React from "react";
import NewMemberSearch from "./NewMemberSearch";

function AddNewMemberButton({ project }) {

    return (
        <NewMemberSearch project={project}/>
    )
};

export default AddNewMemberButton;